var config = {};

// Server General
config.port = 80;

// MySQL Connection
config.database = {
    host: 'localhost',
    user: 'arun',
    password: 'password',
    database: 'MyCCTV'
}

module.exports = config;